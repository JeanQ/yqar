<h1>Listing films</h1>

<table>
  <tr>
    <th>Imdb</th>
    <th>Place</th>
  </tr>

<% for film in @films %>
  <tr>
    <td><%=h film.imdb_id %></td>
    <td><%=h film.place %></td>
    <td><%= link_to 'Show', film %></td>
    <td><%= link_to 'Edit', edit_film_path(film) %></td>
    <td><%= link_to 'Destroy', film, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New film', new_film_path %>
